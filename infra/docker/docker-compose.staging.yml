# Staging. The name below is how Caddy reaches this container, and it carries
# the environment in it so the two stacks can share one network without ever
# answering for each other.
services:
  app:
    environment:
      LANDING_HOSTS: web.replayer.pokerstudio.com.br
    networks:
      edge:
        aliases: [app-staging]
